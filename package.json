{
  "name": "portfolio-server",
  "version": "1.0.0",
  "description": "Mock API for portfolio using json-server",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}